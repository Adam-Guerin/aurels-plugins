[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aurels-hermes"
version = "0.2.0"
description = "Independent Aurels preflight guard for Hermes actions."
requires-python = ">=3.11"
license = {text = "MIT"}
readme = "README.md"

[tool.setuptools]
packages = ["aurels_hermes"]
